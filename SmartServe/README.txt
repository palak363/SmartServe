How to run:
To view the GUI run the 'GUI' class
To view the CLI run the 'CLI' class

Assumptions:
Exit the CLI using the exit option on the main CLI screen for saving the data and retrieving it upon next execution.